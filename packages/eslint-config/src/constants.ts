// Apply rules to unit test files and test related helpers
export const TESTS_GLOB = '**/{tests,__tests__}/**/*.{ts,tsx}';

// Consistent sorting across the board.
// Its off since simple-import-sort doesnt support it!
export const CASE_SENSITIVE = false;
