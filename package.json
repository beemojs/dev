{
  "private": true,
  "name": "beemo-dev",
  "version": "0.0.0",
  "scripts": {
    "build": "packemon build --addEngines",
    "pack": "NODE_ENV=production packemon pack --addEngines --declaration=standard",
    "format": "prettier --write .",
    "type": "tsc --build",
    "prerelease": "yarn run pack && yarn test",
    "release": "lerna publish"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@babel/core": "^7.13.15",
    "@beemo/core": "^2.0.0-rc.0",
    "@beemo/driver-babel": "^2.0.0-rc.0",
    "@beemo/driver-typescript": "^2.0.0-rc.0",
    "@types/babel__core": "^7.1.14",
    "lerna": "^4.0.0",
    "packemon": "^0.15.0",
    "typescript": "^4.2.4"
  }
}
